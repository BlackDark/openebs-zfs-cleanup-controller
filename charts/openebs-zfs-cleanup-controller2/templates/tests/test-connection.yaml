apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "openebs-zfs-cleanup-controller2.fullname" . }}-test-connection"
  labels:
    {{- include "openebs-zfs-cleanup-controller2.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "openebs-zfs-cleanup-controller2.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
