apiVersion: v1
kind: Service
metadata:
  name: {{ include "openebs-zfs-cleanup-controller2.fullname" . }}
  labels:
    {{- include "openebs-zfs-cleanup-controller2.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "openebs-zfs-cleanup-controller2.selectorLabels" . | nindent 4 }}
